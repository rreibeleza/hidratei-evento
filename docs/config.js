// Ajustes do evento. Mudou algo aqui? Suba a versão em sw.js para o tablet pegar.
export const CONFIG = {
  evento: 'Desafio 1 km Hidratei',
  // URL /exec do Apps Script da planilha. Vazio = só guarda no tablet (exportar CSV continua funcionando).
  planilhaUrl: '',
  // Senha simples que o Apps Script confere — só evita lixo de quem achar a URL; não é segredo forte.
  token: '',
  pinPromotora: '2424',
};
